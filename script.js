// const actionButtons = document.getElementById("sticky-buttons");
// const originalOffset = actionButtons.offsetTop;
// 
// window.addEventListener("scroll", () => {
  // if (window.scrollY >= originalOffset - 60) {
    // actionButtons.classList.add("fixed");
  // } else {
    // actionButtons.classList.remove("fixed");
  // }
// });
// 